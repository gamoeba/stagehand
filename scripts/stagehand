#!/bin/bash
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
export LD_LIBRARY_PATH=$DIR/lib
export QT_PLUGIN_PATH=$DIR/plugins

# apply any updates
if [ -d "$DIR/update" ]
then
  cp -avf $DIR/update/* $DIR/ 
  rm -rf $DIR/update
fi
$DIR/bin/stagehand $*
